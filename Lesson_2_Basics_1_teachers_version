//Контракты, вопрос 1
pragma solidity //1. Enter solidity version here

//2. Create contract here


//Переменные, ответ 1, вопрос 2
pragma solidity >=0.5.0 <0.6.0;

contract ZombieFactory {

    //start here

}


//Математические операторы, ответ 2, вопрос 3
pragma solidity >=0.5.0 <0.6.0;

contract ZombieFactory {

    uint dnaDigits = 16;
    //start here

}

//Структуры, ответ 3, вопрос 4
pragma solidity >=0.5.0 <0.6.0;

contract ZombieFactory {

    uint dnaDigits = 16;
    uint dnaModulus = 10 ** dnaDigits;

    // start here

}

//Массивы, ответ 4, вопрос 5
pragma solidity >=0.5.0 <0.6.0;

contract ZombieFactory {

    uint dnaDigits = 16;
    uint dnaModulus = 10 ** dnaDigits;

    struct Zombie {
        string name;
        uint dna;
    }

    // start here

}

//Функции, ответ 5, вопрос 6
pragma solidity >=0.5.0 <0.6.0;

contract ZombieFactory {

    uint dnaDigits = 16;
    uint dnaModulus = 10 ** dnaDigits;

    struct Zombie {
        string name;
        uint dna;
    }

    Zombie[] public zombies;

    // start here

}
//Финальный код и ответ 6
pragma solidity >=0.5.0 <0.6.0;

contract ZombieFactory {

    uint dnaDigits = 16;
    uint dnaModulus = 10 ** dnaDigits;

    struct Zombie {
        string name;
        uint dna;
    }

    Zombie[] public zombies;

    function createZombie (string memory _name, uint _dna) public {
    
    }

}


