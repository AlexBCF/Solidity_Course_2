//версия Solidity
pragma solidity >=0.5.0 <0.6.0;

contract ZombieFactory {
// переменные типа uint - характеристики нашего зомби
    uint dnaDigits = 16;
    uint dnaModulus = 10 ** dnaDigits;
//структура Zombbie
    struct Zombie {
        string name;
        uint dna;
    }
// публичный массив структур Zombie 
    Zombie[] public zombies;
// функция создание нового зомби
    function createZombie (string memory _name, uint _dna) public {
    
    }

}
